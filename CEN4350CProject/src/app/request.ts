export interface SittingRequest {
  id: number;
  requesterId: number;
  petName: string;
  petWeight: number;
  petBreed: string;
  petAge: number;
  address: string;
  date: string;
  startTime: number;
  endTime: number;
  petSitter: number;
}
